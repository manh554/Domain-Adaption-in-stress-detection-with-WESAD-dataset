MODEL:
  NAME: dann_mlp
  METHOD: dann
  BACKBONE: mlp
  HEAD_HIDDEN: 0
  DISC_HIDDEN: 1024
DATA:
  LOADER: wesad_feature
  CSV_PATH: 2.csv
TRAIN:
  EPOCHS: 60
  LR: 1.0e-3
  OPTIMIZER: adam
  WEIGHT_DECAY: 0.0
  LAMBDA_MAX: 0.1
  LAMBDA_WARMUP_EPOCHS: 0
  MODEL_SELECTION: last
